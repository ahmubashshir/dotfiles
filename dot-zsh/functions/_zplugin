#compdef zplugin
_list_plugins()
{
	local -a plugins
	plugins=( ~/.zsh/plugins/*(/) )
	plugins=( ${plugins[@]##*/} )
	_values "plugins" "${plugins[@]}"
}

local args
args=(
    "1:plugin:_list_plugins"
    "*::plugin arguments:"
)
_arguments -s "${args[@]}"
