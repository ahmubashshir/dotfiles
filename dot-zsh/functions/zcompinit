# function zcompinit
if [[ -o interactive ]];then
	compinit $@
	source $HOME/.zsh/compdef
	[[ $* =~ -[[:alnum:]]*C ]] || {
		autoload -Uz zrecompile
		zrecompile
		compinit -C
		source $HOME/.zsh/compdef
	}
fi
unfunction zcompinit
