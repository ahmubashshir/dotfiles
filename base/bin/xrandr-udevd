#!/usr/bin/env mksrc.sh
findpkg = --pkg=$(1)
TYPE    = vala
COMP    = valac
PKGS    = gio-2.0 gudev-1.0 posix xcb-randr xcb x11
FLAGS = -X -Wno-discarded-qualifiers \
		--abi-stability \
		--vapidir $(XDG_DATA_HOME)/vapis-extra
