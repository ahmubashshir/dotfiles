#!/usr/bin/env mksrc.sh
findpkg = --pkg=$(1)
TYPE    = vala
COMP    = valac
PKGS    = gio-2.0 posix
FLAGS = -X -Wno-discarded-qualifiers \
		--abi-stability \
		--vapidir $(XDG_DATA_HOME)/vapis-extra
